{
  "name": "telefs",
  "version": "0.1.1",
  "description": "High-performance virtual filesystem CLI using Telegram Saved Messages as remote storage with AES encryption and deduplication.",
  "main": "bin/telefs.js",
  "bin": {
    "telefs": "./bin/telefs.js"
  },
  "scripts": {
    "postinstall": "python3 -m venv .venv && ./.venv/bin/pip install -r requirements.txt",
    "prepublishOnly": "rm -rf .venv node_modules",
    "start": "node bin/telefs.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "telegram",
    "filesystem",
    "storage",
    "cloud",
    "cli",
    "encryption",
    "deduplication",
    "remote-fs"
  ],
  "author": "Antigravity",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/TODO_USER/telefs.git"
  },
  "bugs": {
    "url": "https://github.com/TODO_USER/telefs/issues"
  },
  "homepage": "https://github.com/TODO_USER/telefs#readme",
  "dependencies": {},
  "engines": {
    "node": ">=12.0.0"
  }
}

